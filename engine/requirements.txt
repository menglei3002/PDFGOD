# PDFGOD Conversion Engine Dependencies
# Core PDF libraries
PyMuPDF>=1.23.0          # PDF rendering, text extraction, fast
pdf2docx>=0.5.8          # PDF to Word conversion
pdfplumber>=0.10.0       # Text extraction fallback
python-docx>=1.1.0       # Word document creation (for OCR output)

# Table extraction
tabula-py>=2.9.0         # PDF table extraction
camelot-py>=0.11.0       # Advanced table extraction
openpyxl>=3.1.0          # Excel file creation

# PPT creation
python-pptx>=0.6.23      # PPTX file creation

# OCR (install separately if needed)
# paddleocr>=2.7.0       # Chinese OCR (heavy, install on demand)
# pytesseract>=0.3.10    # Tesseract fallback (requires system Tesseract install)

# Image processing
Pillow>=10.0.0           # Image handling
